extends layouts/app

block content
  #sidebar
  #additional
  #content-wrapper
    #mobile-nav
      a.toggle-menu <i class="fa fa-fw fa-lg fa-bars"></i>
      a.toggle-user-menu <i class="fa fa-fw fa-lg fa-users"></i>
    #content
  != "<script id='bootstrap' type='text/javascript'>"
  != "window.user = "
  != JSON.stringify(user)
  != ";"
  != "window.users = "
  != JSON.stringify(users)
  != ";"
  != "window.posts = "
  != JSON.stringify(posts)
  != ";"
  != "</script>"
